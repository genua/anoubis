The kernel was installed to /bsd.anoubis

To boot the specific kernel wait until the boot prompt appears and
enter:

	boot> bsd.anoubis

or adapt boot.conf in the following way:

	echo 'set image bsd.anoubis' >> /etc/boot.conf
